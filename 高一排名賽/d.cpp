#include <bits/stdc++.h>
using namespace std;

int dp[3][10000005], s[10000005];

int main() {
	int n;
	cin >> n;
	for (int i=1; i<=n; i++)
		cin >> s[i];
	memset(dp, 0, sizeof dp);

	for (int i=1; i<=n; i++) {
		
		

	return 0;
}

