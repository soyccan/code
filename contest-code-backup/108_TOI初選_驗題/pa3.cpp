#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    string s;
    cin >> s;
    cout <<9;
        cout << ((s == "0" || s == "180") ? 2 : 4) << endl;
        return 0;
}
